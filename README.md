# HMC-Code-NaCl

This repository contains the parallel and sequential hybrid monte-carlo codes used to study NaCl crystallisation from aqueous solutions.

- `HMC_Parallel_Code/` — parallel version
- `HMC_Sequential_Code/` — sequential version

Each folder is self-contained and builds independently via its own `Makefile`.
